"""FastAPI application package for PangyoPass backend.

This package is intentionally small at the beginning and will grow as we add:
- configuration (env-based)
- auth (JWT)
- terms search & saved terms
- database session & models
"""

